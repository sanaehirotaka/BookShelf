@page
@model IndexModel
@{
    ViewData["Title"] = "Home";
}
@foreach (var bucket in Model.Options.ShelfLocations)
{
    <strong>@(bucket.Name)</strong>
    <ul id="thumb">
        @foreach (var obj in Model.List(bucket.Name).OrderBy(x => Random.Shared.Next()).Take(10).OrderBy(x => x.Name))
        {
            <li title="@(obj.Name)">
                <a href="book/@(obj.Id)">
                    <img src="api/bookshelf/thumb/@(obj.Id)" loading="lazy">
                </a>
            </li>
        }
    </ul>
}
